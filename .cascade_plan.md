### Plan
1) Backend: done — /api/annotate accepts script_pdf and builds annotations from text.
2) Frontend: 
    1. (not started) Reproduce/inspect annotation flow (frontend button state, network call, console/logs)
    2. (not started) Verify backend /api/annotate handling, check errors and return payload
    3. (not started) Implement fixes (UI state/validation or backend parsing) and retest
    add PDF input in Video section; send video + script_pdf in Generate Annotations request.
3) Keep render flow: render button already burns annotations to MP4; ensure UI uses new data path.
